import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles((theme) => ({
    root: {

    },
    title: {
        
    },
    tabs: {
        flex: 1,
        paddingLeft: '20%'
    },
    resumeButton: {
        alignItems: 'right'
    }
}));